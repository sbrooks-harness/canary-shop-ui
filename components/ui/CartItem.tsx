import { Card } from "semantic-ui-react";
import classes from "./CartItem.module.css";

const CartItem = () => {
  return <Card className={classes.cartItem}>Cart Item</Card>;
};

export default CartItem;
